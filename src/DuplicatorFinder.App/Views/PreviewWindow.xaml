<Window x:Class="DuplicatorFinder.App.Views.PreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Preview do grupo"
        Height="480" Width="900"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding Items}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="LightGray" BorderThickness="1" Margin="0,0,12,0" Padding="8">
                            <StackPanel Width="320">
                                <Image Source="{Binding FullPath, Converter={StaticResource FilePathToThumbnail}, ConverterParameter=320}"
                                       Height="320" Stretch="Uniform"/>
                                <TextBlock Text="{Binding FileName}" TextWrapping="Wrap" TextAlignment="Center"
                                           FontWeight="Bold" Margin="0,8,0,0"/>
                                <TextBlock Text="{Binding SizeDisplay}" TextAlignment="Center" Foreground="Gray"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
